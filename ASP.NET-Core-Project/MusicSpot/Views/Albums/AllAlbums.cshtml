@model AllAlbumsViewModel

@{
    ViewData["Title"] = "Albums";
}
<br />
<form method="get">
    <div class="form-group col-md-3">
        <label asp-for="SearchTerm"></label>
        <input asp-for="SearchTerm" class="form-control" placeholder="Album name, Year or Format">
    </div>
</form>
<br />
<h1>My Albums</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                ImageUrl
            </th>
            <th>
                Year
            </th>
            <th>
                Format
            </th>
            <th>
                MediaCondition
            </th>
            <th>
                SleeveCondition
            </th>
            <th>
                Notes
            </th>
            <th>
                Artist
            </th>
            <th>
                Tracks
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Albums)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <img src="@Html.Raw(item.ImageUrl)" class="img-thumbnail">
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Format)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MediaCondition)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SleeveCondition)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Notes)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Artist.Name)
                </td>
                <td>
                    <a class="nav-link btn text-center" asp-area="" asp-controller="Tracks" asp-action="AllTracks" asp-route-id="@item.Id">All Tracks</a>
                </td>
                @* <td>
                    <a class="nav-link btn text-center" asp-area="" asp-controller="Tracks" asp-action="Create" asp-route-id="@item.Id">Add Tracks</a>
                    </td>*@
                <td>
                    <a asp-controller="Tracks" asp-action="Create" asp-route-id="@item.Id">Add Tracks</a> ||
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> ||
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> ||
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>